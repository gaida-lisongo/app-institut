'use client';

const RechercheLayout = ({ children }: { children: React.ReactNode }) => {
    
    return (
        <div>
            {children}
        </div>
    );
}

export default RechercheLayout;